# Microsoft Agent Framework (required --pre flag during installation)
agent-framework-azure-ai>=1.0.0b251028

# Azure AI services and authentication
azure-ai-projects
azure-identity
azure-ai-evaluation>=1.0.0

# Excel processing
openpyxl
pandas

# UI components
tksheet>=7.5.0

# Web automation for link checking and HTTP requests
playwright
requests>=2.28.0
urllib3>=1.26.0
aiohttp

# Environment and configuration
python-dotenv

# Azure monitoring and tracing
azure-monitor-opentelemetry
opentelemetry-sdk
opentelemetry-exporter-otlp
azure-core-tracing-opentelemetry

# Testing
pytest>=6.0.0
pytest-asyncio

# Web interface (--web mode)
fastapi>=0.109.0
uvicorn>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.6

# Note: Install agent-framework-azure-ai with --pre flag:
# pip install agent-framework-azure-ai --pre
# Note: Install Playwright browsers with:
# playwright install chromium firefox webkit
# Note: For GUI windowed mode (non-web), install tkinter:
# macOS: brew install python-tk@3.12
# Linux: sudo apt-get install python3-tk
